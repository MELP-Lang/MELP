-- MLP Complete Integration Test
-- Tests: Array + Function Call + Re-assignment + Print

function square(numeric x) returns numeric
    return x * x
end function

function main()
    numeric[] values = [3, 4]
    
    numeric a = values[0]
    numeric b = values[1]
    
    numeric res1 = square(a)
    numeric res2 = square(b)
    numeric total = res1 + res2
    
    print total
    
    return 0
end function
