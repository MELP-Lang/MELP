-- Main program (standalone - will link with external .o files)
function main() returns numeric
    numeric x = 10
    numeric y = 20
    
    -- Simple calculation without external functions for now
    numeric result = x + y + 30
    
    return result
end function
