function max(numeric a, numeric b) returns numeric
    if a > b then
        return a
    else
        return b
    end if
end function

function main() returns numeric
    numeric x = 15
    numeric y = 20
    numeric result = max(x, y)
    numeric output = println(result)
    return 0
end function
