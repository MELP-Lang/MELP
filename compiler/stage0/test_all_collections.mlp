function main() returns numeric
    numeric[] arr = [100, 200, 300]
    list myList = (10; 20; 30;)
    tuple myPair = <42, 99>
    
    numeric a = arr[2]
    numeric b = myList(1)
    numeric c = myPair<0>
    
    numeric result = a + b + c
    return result
end function
