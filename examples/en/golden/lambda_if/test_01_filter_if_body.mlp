-- TEST: Lambda if body in array_filter
-- LANG: LANG-12
-- Expected: 2\n4

numeric[] nums = [1; 2; 3; 4; 5]
numeric[] evens = array_filter(nums; lambda(x) -> if x mod 2 == 0 then 1 else 0 end if)
for each n in evens
    print(n)
end for
