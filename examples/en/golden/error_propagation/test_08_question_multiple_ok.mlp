-- test_08_question_multiple_ok.mlp
-- Expected: 30

enum Result<T, E>
  Ok(T)
  Err(E)
end enum

numeric function compute()
  Result r1 = Result.Ok(10)
  Result r2 = Result.Ok(20)
  numeric a = r1?
  numeric b = r2?
  return a + b
end function

print(compute())
