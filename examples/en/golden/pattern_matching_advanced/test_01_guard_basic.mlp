-- Guard clause basic test
-- Expected: 100

enum Option<T>
  Some(T)
  None
end enum

Option x = Option.Some(42)

match x
  case Some(v) if v > 100 -> print(200)
  case Some(v) if v > 0 -> print(100)
  case Some(v) -> print(50)
  case None -> print(0)
end match
