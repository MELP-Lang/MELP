-- Guard with less than check
-- Expected: 5

enum Option<T>
  Some(T)
  None
end enum

Option x = Option.Some(5)

match x
  case Some(v) if v < 10 -> print(5)
  case Some(v) if v < 100 -> print(50)
  case Some(v) -> print(100)
  case None -> print(0)
end match
