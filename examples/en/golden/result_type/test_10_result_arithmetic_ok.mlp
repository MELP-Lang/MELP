-- Result with arithmetic in Ok value
-- Expected: 15

enum Result<T, E>
  Ok(T)
  Err(E)
end enum

numeric x = 10
numeric y = 5
Result r = Result.Ok(x + y)

match r
  case Ok(v) -> print(v)
  case Err(e) -> print(0)
end match
