-- YZ_93: Test multiple interpolations with mixed expressions
-- Expected output: "Sum: 30, Product: 200, Abs: 10"

function main() returns numeric
    numeric x = 10
    numeric y = 20
    numeric z = -10
    
    -- Multiple interpolations
    string result = "Sum: ${x + y}, Product: ${x * y}, Abs: ${abs(z)}"
    
    println(result)
    
    return 0
end_function
