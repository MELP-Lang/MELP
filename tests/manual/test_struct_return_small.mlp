// YZ_85: Test small struct return (≤16 bytes) - STO: REGISTER return
// Point struct is 16 bytes (2 × numeric = 2 × 8 bytes)
// Expected: RAX + RDX register return

struct Point
    numeric x
    numeric y
end_struct

function create_point(numeric a, numeric b) returns Point
    Point p
    p.x = a
    p.y = b
    return p
end_function

function main() returns numeric
    Point result
    result = create_point(25, 30)
    return result.x + result.y  // Should return 55
end_function
